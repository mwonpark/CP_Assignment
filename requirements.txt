# ====================================================================
# 🎮 비트 바이트 (Beat Byte) 통합 의존성 명세서 (requirements.txt)
# ====================================================================

# 1. 프론트엔드 GUI 및 멀티미디어 코어 (main_ui.py)
# Python 3.11 ~ 3.13 환경과 가장 호환성이 좋은 안정화 버전입니다.
pygame

# 2. 임시 그래픽 에셋 자동 생성 라이브러리 (generate_assets.py)
# PIL(Python Imaging Library)의 최신 후속 안정판입니다.
Pillow>=10.3.0

# 3. 중앙 온라인 순위표 웹 API 서버 코어 (server.py)
# 가볍고 강력한 마이크로 웹 프레임워크입니다.
Flask>=3.0.3

# 4. 리눅스 배포용 고성능 웹 서버 게이트웨이 (Render 클라우드 서버 배포용)
# Windows 환경에서는 설치가 제외되며, 리눅스 기반 Render 환경에서 필수 구동됩니다.
gunicorn>=22.0.0
